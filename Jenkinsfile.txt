	Pipeline {
agent any 
stages{
         stage('Build') {
              bat "mvn clean test"

         }
         stage('Package') {
              bat "mvn package"

         }
		 stage('test') {
              junit '**/target/surefire-reports/TEST-*.xml'
             archive 'target/*.jar'
         }
         }
		 }
